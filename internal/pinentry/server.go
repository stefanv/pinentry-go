// Package pinentry implements the Assuan protocol server for pinentry-go.
package pinentry

import "io"

// Serve reads Assuan commands from r and writes responses to w.
// This is a placeholder; full implementation follows in Phase 3.
func Serve(r io.Reader, w io.Writer) error {
	panic("not yet implemented")
}
